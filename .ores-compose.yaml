schema_version: ores.compose.v1
project: fanwaave-mcp-server.rs
allow_lazy_start: false

services:
  fanwaave-mcp-server.rs:
    name: fanwaave-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/fanwaave-mcp-server.rs"]
