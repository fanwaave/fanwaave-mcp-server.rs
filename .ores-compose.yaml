schema_version: ores.compose.v1
project: fanwaave-mcp-server
allow_lazy_start: false

services:
  fanwaave-mcp-server:
    name: fanwaave-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/fanwaave-mcp-server.rs"]
